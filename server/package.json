{
  "name": "solid-lighting-backend",
  "version": "1.0.0",
  "description": "Backend for SOLID — Lighting Everything. Serves the static site, captures enquiries (JSON store), emails notifications, and exposes a protected admin API.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "nodemailer": "^8.0.8"
  }
}
